# Tasks: shared:core:time — Centralised Time & Logical Date Module

**Created**: 2026-04-03
**Feature**: `shared:core:time`
**Branch**: `003-add-core-time`
**Spec**: [spec.md](./spec.md) | **Plan**: [plan.md](./plan.md) | **Contract**: [contracts/time-provider-api.md](./contracts/time-provider-api.md)

How to read: Tasks are ordered by dependency — earlier tasks must be complete before later ones begin.
All source paths are relative to the repository root `/Users/platinum/AndroidStudioProjects/Mudawama`.
`[P]` = safe to run in parallel with other `[P]` tasks in the same phase once phase prerequisites are met.
`[US#]` = the user story this task primarily delivers.

---

## Phase 1 — Setup (Gradle & Module Scaffold)

> **Goal**: Register the new Gradle module and wire `kotlinx-datetime` into the version catalog. These three tasks must be completed — in order — before any source code can be written.
>
> **Independent test**: `./gradlew :shared:core:time:help` exits without "Project not found" error.

- [ ] T001 Add `kotlinxDatetime = "0.6.2"` to the `[versions]` block and `kotlinx-datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "kotlinxDatetime" }` to the `[libraries]` block in `gradle/libs.versions.toml`

- [ ] T002 Add `include(":shared:core:time")` to `settings.gradle.kts` — insert it after the existing `include(":shared:core:database")` line so the typesafe project accessor `projects.shared.core.time` is generated by Gradle's type-safe accessors feature in `settings.gradle.kts`

- [ ] T003 Create `shared/core/time/build.gradle.kts` with the following content: apply `id("mudawama.kmp.library")` (KMP + Android + iOS targets) and `id("mudawama.kmp.koin")` (injects `koin-bom` + `koin-core` into `commonMain` and `koin-android` into `androidMain`); configure `android { namespace = "io.github.helmy2.mudawama.core.time" }`; call `configureIosFramework("MudawamaCoreTime")`; declare `commonMain.dependencies { implementation(libs.kotlinx.datetime) }` in `shared/core/time/build.gradle.kts`

---

## Phase 2 — Foundational (Core Types & Rollover Algorithm)

> **Goal**: Define the two shared types that every implementation and every test in this module depends on. Both tasks touch independent files and can be implemented simultaneously.
>
> **Prerequisite**: T001–T003 complete.
> **Independent test**: The module compiles with `./gradlew :shared:core:time:compileCommonMainKotlinMetadata` (zero errors).

- [ ] T004 [P] Create `TimeProvider.kt` declaring `interface TimeProvider` with two methods: `fun nowInstant(): Instant` (returns the current moment; implementations MUST be the only location that reads the system clock — SC-002) and `fun logicalDate(timeZone: TimeZone = TimeZone.currentSystemDefault()): LocalDate` (returns the logical date under the active `RolloverPolicy`); add full KDoc as specified in `contracts/time-provider-api.md` §2; imports: `kotlinx.datetime.Instant`, `kotlinx.datetime.LocalDate`, `kotlinx.datetime.TimeZone`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/TimeProvider.kt`

- [ ] T005 [P] Create `RolloverPolicy.kt` containing: (1) `data class RolloverPolicy(val offsetHour: Int)` with `init { require(offsetHour in 0..23) { "offsetHour must be in 0..23" } }` and a `companion object` exposing `val Standard = RolloverPolicy(0)` and `fun fixed(hour: Int): RolloverPolicy = RolloverPolicy(hour)`; (2) package-level `internal fun computeLogicalDate(calendarDate: LocalDate, hour: Int, policy: RolloverPolicy): LocalDate` implementing the hemisphere-branching algorithm exactly: `val h = policy.offsetHour; return when { h >= 12 && hour >= h -> calendarDate.plus(1, DateTimeUnit.DAY); h in 1..11 && hour < h -> calendarDate.minus(1, DateTimeUnit.DAY); else -> calendarDate }`; add full KDoc per `contracts/time-provider-api.md` §1 and §7; imports: `kotlinx.datetime.LocalDate`, `kotlinx.datetime.DateTimeUnit`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/RolloverPolicy.kt`

---

## Phase 3 — User Story 1: Logical-Date Rollover (P1)

> **Story**: Habit logging records the correct logical date.
> **Independent test**: `SystemTimeProvider(RolloverPolicy.fixed(18)).logicalDate(tz)` when the current wall-clock hour ≥ 18 returns tomorrow's calendar date.
> **Prerequisite**: T004, T005.

- [ ] T006 [US1] Create `SystemTimeProvider.kt` — `class SystemTimeProvider(private val policy: RolloverPolicy) : TimeProvider`; implement `override fun nowInstant(): Instant = Clock.System.now()` (this is the **sole** call site for `Clock.System` in the entire codebase — enforced by SC-002 grep in CI); implement `override fun logicalDate(timeZone: TimeZone): LocalDate` by calling `val ldt = nowInstant().toLocalDateTime(timeZone)` then `return computeLogicalDate(ldt.date, ldt.hour, policy)`; add KDoc per `contracts/time-provider-api.md` §3 noting it is the only `Clock.System` access point; imports: `kotlinx.datetime.*`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/SystemTimeProvider.kt`

---

## Phase 4 — User Story 2: Frozen-Time Test Double (P2)

> **Story**: Unit tests freeze time to verify business logic.
> **Independent test**: `FakeTimeProvider(fixedInstant = Instant.parse("2026-04-06T21:00:00Z"), policy = RolloverPolicy.fixed(18)).logicalDate(TimeZone.UTC)` == `LocalDate(2026, 4, 7)`; same call a second time returns an identical result.
> **Prerequisite**: T004, T005. Can run in parallel with T006.

- [ ] T007 [US2] Create `FakeTimeProvider.kt` — `class FakeTimeProvider(var fixedInstant: Instant, private val policy: RolloverPolicy = RolloverPolicy.Standard) : TimeProvider`; `override fun nowInstant(): Instant = fixedInstant` (no clock access); `override fun logicalDate(timeZone: TimeZone): LocalDate` by calling `val ldt = fixedInstant.toLocalDateTime(timeZone)` then `return computeLogicalDate(ldt.date, ldt.hour, policy)`; `fixedInstant` MUST be `var` (mutable) to support time-travel scenarios in tests (BC-4 in contract); prepend full KDoc `⚠️ For test use only. Do not bind in production Koin modules. Ships in commonMain so consumer modules can use it in their own commonTest without additional Gradle dependency wiring.`; imports: `kotlinx.datetime.*`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/FakeTimeProvider.kt`

- [ ] T008 [US2] Create `LogicalDateCalculatorTest.kt` using `kotlin.test` with the following test cases — all driven via `FakeTimeProvider` seeded with a fixed `Instant` at `TimeZone.UTC` to eliminate clock dependency: (1) US1-Scenario-1: H=18, hour=21 → +1 day (`Instant.parse("2026-04-06T21:00:00Z")`→`LocalDate(2026,4,7)`); (2) US1-Scenario-2: H=18, hour=17 → same day (`"2026-04-06T17:59:00Z"`→`LocalDate(2026,4,6)`); (3) US1-Scenario-3: H=0, hour=23 → same day (`"2026-04-06T23:59:00Z"`→`LocalDate(2026,4,6)`); (4) US1-Scenario-4: H=0, hour=0 → same day (`"2026-04-07T00:01:00Z"`→`LocalDate(2026,4,7)`); (5) US1-Scenario-5: H=3, hour=2 → -1 day (`"2026-04-07T02:00:00Z"`→`LocalDate(2026,4,6)`); (6) Exact boundary inclusive: H=18, 18:00:00 exactly → +1 day (`"2026-04-06T18:00:00Z"`→`LocalDate(2026,4,7)`); (7) Standard equals H=0: `RolloverPolicy.Standard` and `RolloverPolicy(0)` produce identical results for the same instant; (8) Morning-rollover after offset: H=6, hour=10 → same day, H=6, hour=5 → -1 day; (9) Time-travel: mutate `FakeTimeProvider.fixedInstant` mid-test and assert new date; (10) DST edge case: use timezone `TimeZone.of("Europe/London")` with a known spring-forward instant (e.g., `"2026-03-29T01:30:00Z"`) and verify `logicalDate()` does not throw and returns a valid `LocalDate`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonTest/kotlin/io/github/helmy2/mudawama/core/time/LogicalDateCalculatorTest.kt`

---

## Phase 5 — User Story 3: ISO Date Formatting (P3)

> **Story**: Database layer formats dates consistently as `yyyy-MM-dd`.
> **Independent test**: `toIsoDateString(LocalDate(2026, 1, 7))` == `"2026-01-07"`.
> **Prerequisite**: T003 (kotlinx-datetime on classpath). Can be implemented in parallel with Phases 3 and 4.

- [ ] T009 [P] [US3] Create `DateFormatters.kt` with two top-level functions in package `io.github.helmy2.mudawama.core.time`: `fun toIsoDateString(date: LocalDate): String = date.toString()` (delegates to `LocalDate.toString()` which is documented by kotlinx-datetime to produce `yyyy-MM-dd`; add KDoc: "Output is always yyyy-MM-dd, ISO-8601 compliant, lexicographically sortable, suitable for Room TEXT columns"); `fun toIsoDateString(instant: Instant, timeZone: TimeZone): String = instant.toLocalDateTime(timeZone).date.toString()` (add KDoc describing timezone conversion); imports: `kotlinx.datetime.Instant`, `kotlinx.datetime.LocalDate`, `kotlinx.datetime.TimeZone`, `kotlinx.datetime.toLocalDateTime` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/DateFormatters.kt`

- [ ] T010 [P] [US3] Create `DateFormattersTest.kt` using `kotlin.test` with: (1) `toIsoDateString(LocalDate(2026, 1, 7))` == `"2026-01-07"` (US3-Scenario-1); (2) `toIsoDateString(LocalDate(2026, 12, 31))` == `"2026-12-31"`; (3) `toIsoDateString(Instant.parse("2026-12-31T23:00:00Z"), TimeZone.UTC)` == `"2026-12-31"` (US3-Scenario-2); (4) `toIsoDateString(Instant.parse("2026-12-31T23:30:00Z"), TimeZone.of("Asia/Riyadh"))` == `"2027-01-01"` (UTC+3 pushes the date into the next day); package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonTest/kotlin/io/github/helmy2/mudawama/core/time/DateFormattersTest.kt`

---

## Phase 6 — User Story 4: Koin DI Wiring (P4)

> **Story**: Feature modules receive `TimeProvider` from Koin with no manual wiring.
> **Independent test**: `startKoin { modules(timeModule()) }; koin.get<TimeProvider>() === koin.get<TimeProvider>()` — same singleton instance returned.
> **Prerequisite**: T006 (SystemTimeProvider to bind), T004 (TimeProvider interface). Can start after T006.

- [ ] T011 [US4] Create `di/TimeModule.kt` — top-level function `fun timeModule(rolloverPolicy: RolloverPolicy = RolloverPolicy.Standard): Module = module { single<TimeProvider> { SystemTimeProvider(rolloverPolicy) } }`; the policy is closed over at module-creation time (no `parametersOf()` needed at injection sites — FR-011, Decision 3 in research.md); add KDoc with a usage example showing both `timeModule()` (Standard) and `timeModule(RolloverPolicy.fixed(18))` (Islamic rollover); note the default parameter satisfies US4-Scenario-2; imports: `io.github.helmy2.mudawama.core.time.RolloverPolicy`, `io.github.helmy2.mudawama.core.time.SystemTimeProvider`, `io.github.helmy2.mudawama.core.time.TimeProvider`, `org.koin.core.module.Module`, `org.koin.dsl.module`; package `io.github.helmy2.mudawama.core.time.di` in `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/di/TimeModule.kt`

- [ ] T012 [US4] Create `TimeModuleTest.kt` using `kotlin.test` + Koin's `startKoin`/`stopKoin` from `koin-core`: `@BeforeTest fun setup() { startKoin { modules(timeModule()) } }`; `@AfterTest fun teardown() { stopKoin() }`; test `timeProviderIsSingleton`: `val koin = GlobalContext.get(); assertTrue(koin.get<TimeProvider>() === koin.get<TimeProvider>())`; test `defaultModuleResolvesWithoutError`: resolve `get<TimeProvider>()`, call `nowInstant()` and `logicalDate()` — both must return non-null without throwing; test `customPolicyModuleLoads`: `stopKoin(); startKoin { modules(timeModule(RolloverPolicy.fixed(18))) }` — `get<TimeProvider>()` resolves without error; imports: `org.koin.core.context.*`, `org.koin.core.get`, `kotlin.test.*`; package `io.github.helmy2.mudawama.core.time` in `shared/core/time/src/commonTest/kotlin/io/github/helmy2/mudawama/core/time/TimeModuleTest.kt`

- [ ] T013 [US4] Wire the new module into `shared/umbrella-core/build.gradle.kts`: add `api(projects.shared.core.time)` to the `commonMain.dependencies` block alongside the existing `api(projects.shared.core.domain)` and `api(projects.shared.core.data)` entries; also add `export(projects.shared.core.time)` inside the `configureIosFramework` lambda so `TimeProvider` and `RolloverPolicy` are accessible from Swift/ObjC via the MudawamaCore XCFramework in `shared/umbrella-core/build.gradle.kts`

---

## Final Phase — Polish & CI Validation

> **Goal**: Confirm all success criteria (SC-001 through SC-004) are met before the branch is merged.
> **Prerequisite**: All prior tasks complete (T001–T013).

- [ ] T014 [P] Verify SC-002 compliance — run `git grep -rn "Clock\.System" --include="*.kt" -- shared/` from the repository root and confirm the **only** match is `shared/core/time/src/commonMain/kotlin/io/github/helmy2/mudawama/core/time/SystemTimeProvider.kt`; if any other file in `shared/` matches, refactor that call site to receive `TimeProvider` via constructor injection; document the grep command as a comment in `shared/core/time/build.gradle.kts` for future CI wiring in `shared/core/time/build.gradle.kts`

- [ ] T015 [P] Smoke-build both affected modules: run `./gradlew :shared:core:time:compileCommonMainKotlinMetadata` (confirms T003–T011 compile); then run `./gradlew :shared:umbrella-core:compileCommonMainKotlinMetadata` (confirms T013 project accessor resolves); fix any import or namespace errors before proceeding to T016

- [ ] T016 Full test run and SC-001 verification: execute `./gradlew :shared:core:time:allTests` and confirm all tests in `LogicalDateCalculatorTest` (10 cases — SC-004 branch coverage), `DateFormattersTest` (4 cases), and `TimeModuleTest` (3 cases) pass on Android JVM; record total execution time and confirm it is under 5 seconds (SC-001); if iOS Simulator runner is available, also execute `./gradlew :shared:core:time:iosSimulatorArm64Test` and confirm identical pass/fail outcome

---

## Dependencies (Completion Order)

```
T001 ─┐
T002 ─┴──→ T003 ──→ T004 [P] ─────────────────────┬──→ T006 [US1] ──→ T011 [US4] ──→ T012 [US4]
                    T005 [P] ─────────────────────┤                             └──→ T013 [US4]
                                                   ├──→ T007 [US2] ──→ T008 [US2]
                                                   └──→ T009 [P][US3] ──→ T010 [P][US3]

T003 ──→ T013 [US4]   (project accessor available as soon as module is registered)
T006 ──→ T014 [P]     (SC-002 grep needs SystemTimeProvider to exist)
T008 + T010 + T012 ──→ T015 [P] ──→ T016
```

**Critical path** (longest blocking chain):
`T001 → T002 → T003 → T005 → T007 → T008 → T015 → T016`

---

## Parallel Execution Examples

**After T003 completes** (module scaffold + version catalog entry ready):
- `T004` (TimeProvider interface) and `T005` (RolloverPolicy + algorithm) can be developed simultaneously on separate files.

**After T004 + T005 complete** (core types available):
- `T006` (SystemTimeProvider — US1) + `T007` (FakeTimeProvider — US2) + `T009` (DateFormatters — US3) can all start in parallel, touching three independent files.

**After T007 completes** (test double available):
- `T008` (LogicalDateCalculatorTest) can start immediately, independent of T006 and T009.

**After T009 completes**:
- `T010` (DateFormattersTest) can start immediately.

**After T006 completes**:
- `T011` (TimeModule) can start — requires SystemTimeProvider to be bindable.
- `T014` (SC-002 grep) can run as a standalone CI-style check.

**After T011 completes**:
- `T012` (TimeModuleTest) and `T013` (umbrella-core `api()` wiring) can run in parallel.

---

## Implementation Strategy

**MVP Scope** — delivers US1 (the primary correctness guarantee) and US2 (reliable test harness):
`T001 → T002 → T003 → T004 + T005 → T006 → T007 → T008`

After T008 the rollover algorithm is implemented, the test double is available for all consumer modules, and the 10-case test suite validates 100 % branch coverage (SC-004). All other stories build on this foundation without blocking it.

**Incremental Delivery Order**:

| Sprint | Tasks | Deliverable |
|--------|-------|-------------|
| 1 — Foundation | T001–T005 | Gradle module + core types; unblocks all parallel work |
| 2 — MVP | T006–T008 | Real clock impl + frozen-time double + full rollover test suite (US1 + US2) |
| 3 — Formatting + DI | T009–T013 | Date formatters, Koin module, umbrella-core wiring (US3 + US4); all parallelisable |
| 4 — Polish | T014–T016 | SC-002 grep, smoke build, full test run; confirms all success criteria |

---

## Output Summary

| Metric | Value |
|--------|-------|
| **Total tasks** | 16 (T001–T016) |
| Phase 1 — Setup | 3 tasks (T001–T003) |
| Phase 2 — Foundational | 2 tasks (T004–T005) |
| Phase 3 — US1 (P1) | 1 task (T006) |
| Phase 4 — US2 (P2) | 2 tasks (T007–T008) |
| Phase 5 — US3 (P3) | 2 tasks (T009–T010) |
| Phase 6 — US4 (P4) | 3 tasks (T011–T013) |
| Final — Polish | 3 tasks (T014–T016) |
| **Parallelisable tasks** | 8 (`[P]`: T004, T005, T009, T010, T014, T015; US-level: T006 ∥ T007 ∥ T009 after T004+T005) |
| **Parallel opportunities** | 3 identified (see section above) |
| **MVP scope** | T001–T008 (Phases 1–4) |

**Independent test criteria per story**:

| Story | Criterion |
|-------|-----------|
| US1 (P1) | `FakeTimeProvider(Instant.parse("2026-04-06T21:00:00Z"), RolloverPolicy.fixed(18)).logicalDate(TimeZone.UTC)` == `LocalDate(2026, 4, 7)` |
| US2 (P2) | Same `FakeTimeProvider` called twice returns `==` result; mutating `fixedInstant` yields a new deterministic result |
| US3 (P3) | `toIsoDateString(LocalDate(2026, 1, 7))` == `"2026-01-07"` |
| US4 (P4) | `startKoin { modules(timeModule()) }; koin.get<TimeProvider>() === koin.get<TimeProvider>()` |

---

## Format Validation

All 16 tasks follow the required checklist format:
- ✅ Checkbox (`- [ ]`)
- ✅ Sequential Task ID (`T001`–`T016` in execution order)
- ✅ `[P]` marker on all parallelisable tasks (T004, T005, T009, T010, T014, T015)
- ✅ `[US#]` label on every user-story-phase task (T006→US1, T007–T008→US2, T009–T010→US3, T011–T013→US4)
- ✅ Explicit file path in every task description
- ✅ Setup and Polish phase tasks carry no story label (correct per rules)

